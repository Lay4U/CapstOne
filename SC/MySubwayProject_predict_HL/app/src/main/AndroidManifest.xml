<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mysubwayproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".model"></activity>
        <activity android:name=".DateSetter" />
        <activity android:name=".TimeSetter" />
        <activity android:name=".ResultView" />
        <activity android:name=".StartStation" />
        <activity android:name=".EndStation" /> <!-- 스플래시 액티비티 -->
        <activity
            android:name=".splashActivity"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 메인 액티비티 -->
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>